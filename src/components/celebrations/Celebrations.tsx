import React from 'react';
import { Box, Typography } from '@mui/material';

const Celebrations: React.FC = () => {
  return (
    <Box>
      <Typography variant="h6">Celebrations</Typography>
    </Box>
  );
};

export default Celebrations;